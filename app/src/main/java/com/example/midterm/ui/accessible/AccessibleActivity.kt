package com.example.midterm.ui.accessible

import android.os.Bundle
import com.example.midterm.databinding.ActivityAccessibleBinding
import com.example.midterm.ui.base.BaseActivity

class AccessibleActivity : BaseActivity<ActivityAccessibleBinding>(ActivityAccessibleBinding::inflate) {

    override fun onCreate(savedInstanceState: Bundle?) {
        super.onCreate(savedInstanceState)
        // TODO: wire Cart / Voucher / Checkout navigation for Accessible mode
    }
}
